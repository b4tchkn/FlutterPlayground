import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class BagPage extends StatelessWidget {
  final Color color;
  BagPage(this.color);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("This is Bag"),
    );
  }
}
